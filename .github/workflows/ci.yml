name: CI

on:
  push:
    branches: [mob]
  pull_request:
    branches: [mob]

env:
  REGISTRY: ghcr.io
  IMAGE_NAME: ${{ github.repository_owner }}/tinycc-armv8m

jobs:
  build-and-test:
    runs-on: ubuntu-latest
    container:
      image: ghcr.io/${{ github.repository_owner }}/tinycc-armv8m:latest
      options: --user root
      credentials:
        username: ${{ github.actor }}
        password: ${{ secrets.GITHUB_TOKEN }}

    steps:
      - name: Checkout repository
        uses: actions/checkout@v4
        with:
          submodules: recursive

      - name: Configure
        run: ./configure --enable-O2 --enable-asan

      - name: Build and test
        run: |
          virtualenv .venv
          source .venv/bin/activate
          make test -j$(nproc)
